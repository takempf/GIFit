---
trigger: always_on
---

ALWAYS ensure that newly authored code is valid typescript according to our tsconfig and eslint rules. Validate using `npm run validate`, `npm run lint`, or manual validation commands (such as tsc) before returning to the user.
